{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1> Nuevo Evento </h1>

                {{ form_start(eventosForm) }}
                {{ form_widget(eventosForm) }}

                <button type="submit" class="btn btn-primary">Guardar</button>
                <a class="btn btn-primary" href="{{ path('list_eventos') }}">Regresar</a>
                {{ form_end(eventosForm) }}
            </div>
        </div>
    </div>
{% endblock %}